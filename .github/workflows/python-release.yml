name: Release

on:
  workflow_call:
    outputs:
      released:
        description: "Whether a new version was released"
        value: ${{ jobs.release.outputs.released }}

jobs:
  release:
    runs-on: ubuntu-latest
    concurrency: release
    permissions:
      contents: write
    outputs:
      released: ${{ steps.release.outputs.released }}

    steps:
      - uses: actions/create-github-app-token@v3
        id: app-token
        with:
          client-id: ${{ secrets.APP_CLIENT_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}

      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          token: ${{ steps.app-token.outputs.token }}

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: pip install ".[release]"

      - name: Configure git
        run: |
          git config user.name "repokit"
          git config user.email "repokit@djachenko"

      - name: Run semantic release
        id: release
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          TAG_BEFORE=$(git tag --sort=-version:refname | head -1)
          semantic-release version
          TAG_AFTER=$(git tag --sort=-version:refname | head -1)
          if [ "$TAG_BEFORE" != "$TAG_AFTER" ]; then
            echo "released=true" >> $GITHUB_OUTPUT
            echo "version=${TAG_AFTER#v}" >> $GITHUB_OUTPUT
          else
            echo "released=false" >> $GITHUB_OUTPUT
          fi

      - name: Build
        if: steps.release.outputs.released == 'true'
        run: python -m build

      - name: Upload dist
        if: steps.release.outputs.released == 'true'
        uses: actions/upload-artifact@v7
        with:
          name: dist
          path: dist/
