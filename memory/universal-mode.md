# universal-mode — Рефакторинг скрипта для поддержки существующих репозиториев

<!-- Проектируем универсальный режим: линейный пайплайн с gate-проверками вместо только new-repo сценария. -->

### 2026-06-09 — Реализация универсального пайплайна, тест на goodoc

Реализован линейный пайплайн с gate-проверками. repo.sh разбит на git_init.sh + remote.sh. workflows.sh и pyproject.sh пропускают существующие файлы. ruleset.sh — GET → DELETE + POST (PATCH через API не работает). Имя рулсета: `djachenko-github-flow-ruleset`. Протестировано на goodoc (сценарий: оба есть → только настройка).

Не протестированы: (2) нет локального git, нет GitHub репо; (3) есть локальный git, нет GitHub репо.

**Бэклог:**
- Протестировать сценарии 2 и 3 (новое репо с нуля; локальный git без remote)
- Наборы шаблонов по стекам — `templates/python/`, `templates/swift/`, `templates/bare/`. Ядро универсальное, шаблоны подменяемые. Выбор набора при запуске.
