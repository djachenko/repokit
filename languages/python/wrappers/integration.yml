name: Integration

on:
  pull_request:
    branches:
      - master

jobs:
  integration:
    uses: djachenko/repokit/.github/workflows/python-integration.yml@{{VERSION}}
    with:
      package_name: {{REPO}}
    secrets: inherit
    permissions:
      id-token: write
