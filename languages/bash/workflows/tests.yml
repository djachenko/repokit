name: Tests

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: shellcheck
        uses: shellcheck-py/shellcheck-py@v0.11.0.1
        with:
          scandir: '.'

      - name: shfmt
        uses: mfinelli/setup-shfmt@v4.0.1
      - run: shfmt -d .
